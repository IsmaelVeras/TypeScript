// Função que recebe um número e retorna true se for positivo ou false se for negativo

function numeroTrueOrFalse(numeroEntrada: number): boolean {
  return numeroEntrada >= 0;
}

console.log(numeroTrueOrFalse(-8))
